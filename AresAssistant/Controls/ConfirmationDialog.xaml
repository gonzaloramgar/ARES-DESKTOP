<Window x:Class="AresAssistant.Controls.ConfirmationDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="ARES — Confirmación"
        Width="420" Height="250"
        WindowStyle="None" AllowsTransparency="True"
        Background="Transparent"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize">

    <Border Background="#0d0d0d" CornerRadius="10"
            BorderBrush="#ff2222" BorderThickness="2">
        <Grid Margin="20">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,12">
                <Path Data="M 8,0 L 16,11 L 8,22 L 0,11 Z"
                      Fill="#ff2222" Width="14" Height="20" Margin="0,0,8,0"/>
                <TextBlock Text="Confirmación requerida"
                           FontSize="15" FontWeight="Bold"
                           Foreground="#ff2222" FontFamily="Consolas"/>
            </StackPanel>

            <Border Grid.Row="1" Background="#1a1a1a" CornerRadius="6" Padding="12">
                <StackPanel>
                    <TextBlock Foreground="#aaaaaa" FontSize="12" Margin="0,0,0,6">
                        <Run Text="Herramienta: "/>
                        <Run x:Name="ToolNameRun" FontWeight="Bold" Foreground="#e0e0e0"/>
                    </TextBlock>
                    <TextBlock x:Name="ArgsText" Foreground="#888888" FontSize="11"
                               TextWrapping="Wrap" FontFamily="Consolas"/>
                </StackPanel>
            </Border>

            <StackPanel Grid.Row="2" Orientation="Horizontal"
                        HorizontalAlignment="Right" Margin="0,16,0,0">
                <Button Content="Cancelar" Style="{StaticResource GhostButton}"
                        Width="90" Click="Cancel_Click" Margin="0,0,8,0"/>
                <Button Content="Aprobar" Style="{StaticResource AresButton}"
                        Width="90" Click="Approve_Click"/>
            </StackPanel>
        </Grid>
    </Border>
</Window>
